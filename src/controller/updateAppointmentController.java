package controller;

public class updateAppointmentController {

    //extract start and end date with Timestamp ts = rs.getTimestamp("column name") in query
    // use DateTimeFormatter.ofPattern to make a date/time readable
}
